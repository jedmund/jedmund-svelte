[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare pnpm@9.15.9 --activate",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = ["./scripts/railway-build.sh"]

[start]
cmd = "pnpm run start"
